const api = '/api/';

export const URL = {
    products: `${api}products`,
    register: `${api}auth/register`,
    login: `${api}auth/login`,
    categories: `${api}categories`,
    orders: `${api}orders`,
    account: `${api}account`
}
